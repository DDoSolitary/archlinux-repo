diff --git a/.SRCINFO b/.SRCINFO
index 767d7d1..82a1485 100644
--- a/.SRCINFO
+++ b/.SRCINFO
@@ -8,7 +8,8 @@ pkgbase = waifu2x-converter-cpp-cuda-git
 	makedepends = cmake
 	makedepends = git
 	makedepends = opencl-headers
-	depends = opencv-cuda
+	depends = opencv
+	depends = cuda
 	depends = opencl-icd-loader
 	provides = waifu2x-converter-cpp
 	conflicts = waifu2x-converter-cpp
diff --git a/PKGBUILD b/PKGBUILD
index 4b287dc..4b85cce 100644
--- a/PKGBUILD
+++ b/PKGBUILD
@@ -10,7 +10,8 @@ pkgdesc="Image Super-Resolution for Anime-Style-Art. (re-implementation in C++ u
 arch=('x86_64')
 url='https://github.com/DeadSix27/waifu2x-converter-cpp'
 license=('MIT')
-depends=('opencv-cuda'
+depends=('opencv'
+         'cuda'
          'opencl-icd-loader'
          )
 makedepends=('cmake'
